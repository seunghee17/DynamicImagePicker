<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- API 26-32: READ_EXTERNAL_STORAGE -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- API 32-: READ_EXTERNAL_STORAGE (partial media access not available) -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>
    <uses-permission android:name="android.permission.READ_MEDIA_VISUAL_USER_SELECTED" />

    <application>
        <!--
            라이브러리 전용 FileProvider.
            authority = "${applicationId}.imagepicker.fileprovider"
            호스트 앱 FileProvider와 authority가 다르므로 충돌 없음.
        -->
        <provider
            android:name="io.github.seunghee17.imagepicker.ImagePickerFileProvider"
            android:authorities="${applicationId}.imagepicker.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/imagepicker_file_paths" />
        </provider>
    </application>

</manifest>
